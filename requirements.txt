# Dependências Python — Auto Tools
# Instale com: pip install -r requirements.txt
# Ou crie o venv com: python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt

# === Autenticação e Criptografia ===
bcrypt>=4.1.0               # Hash de senhas de usuário
cryptography>=41.0.0        # Fernet (AES-128-CBC) para cofre de senhas
python-dotenv>=1.0.0        # Leitura de variáveis de ambiente (.env)

# === Google API (Gmail, Drive) ===
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0

# === Processamento de Dados ===
openpyxl>=3.1.0             # Leitura/escrita de Excel (.xlsx)
duckdb>=0.10.0              # Consultas analíticas em arquivos grandes
pyarrow>=14.0.0             # Suporte a Parquet
pandas>=2.1.0               # Manipulação de DataFrames
polars>=1.40.1              # Processamento de dados
fastexcel>=0.20.1           # Leitura/escrita de Excel (.xlsx)
xlrd>=2.0.2                 # Leitura de Excel (.xls)

# === Automação Web ===
playwright>=1.40.0          # Browser automation (substitui Selenium)
selenium>=4.15.0            # Automação legado
webdriver-manager>=4.0.0    # Gerenciamento de drivers Selenium
requests>=2.31.0            # Requisições HTTP

# === Utilitários ===
Pillow>=10.0.0              # Manipulação de imagens (se necessário)

# === Cloud / Firebase ===
firebase-admin>=6.4.0       # Integração com Google Firebase (Nuvem)
